{
  "name": "aizamo-root",
  "private": true,
  "engines": { "node": "20.x" },
  "scripts": {
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm --prefix frontend ci && npm --prefix frontend run build && rm -rf build && mv frontend/build build"
  }
}
